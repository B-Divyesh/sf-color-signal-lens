[
  {
    "id": "sample-lens",
    "claim": "Try it with sample data opens a diff lens with nothing saved.",
    "where": "landing hero, demo banner",
    "test": "npm test -- --grep @claim:sample-lens",
    "sandbox": "fresh browser context at /demo"
  },
  {
    "id": "local-screenshots",
    "claim": "Screenshots are read on your device; the demo sends no screenshot or analytics request.",
    "where": "privacy page, workspace permission note",
    "test": "npm test -- --grep @claim:local-screenshots",
    "sandbox": "fresh browser context at /demo; intercept all requests during lens use"
  },
  {
    "id": "reading-cues",
    "claim": "The free lens reads screenshots with labels, patterns, or blue-orange remapping.",
    "where": "landing preview, workspace controls",
    "test": "npm test -- --grep @claim:reading-cues",
    "sandbox": "fresh browser context at /demo; switch each cue"
  },
  {
    "id": "demo-reset",
    "claim": "Reset demo replaces demo data without touching real data.",
    "where": "demo banner",
    "test": "npm test -- --grep @claim:demo-reset",
    "sandbox": "fresh browser context at /demo; reset local demo namespace"
  },
  {
    "id": "screenshot-input",
    "claim": "Works from a screenshot opened from your device.",
    "where": "landing facts, workspace",
    "test": "npm test -- --grep @claim:screenshot-input",
    "sandbox": "fresh browser context at /lens; open a bundled PNG fixture and inspect the source name"
  },
  {
    "id": "capture-consent",
    "claim": "Screen capture is requested only after Capture screen region is pressed, and only the selected region is added.",
    "where": "landing how-it-works, workspace permission note, README, privacy page",
    "test": "npm test -- --grep @claim:capture-consent",
    "sandbox": "fresh browser context with a mocked display picker; select coordinates and assert the selected region is used"
  },
  {
    "id": "named-presets",
    "claim": "Lens Plus saves, lists, applies, renames, and deletes named presets on this device.",
    "where": "landing Lens Plus section, workspace",
    "test": "npm test -- --grep @claim:named-presets",
    "sandbox": "fresh browser context with a valid cached license; save, reload, apply, rename, delete, and reload again"
  },
  {
    "id": "license-entitlement",
    "claim": "Lens Plus controls appear only after an active license has been verified.",
    "where": "Lens Plus purchase section and workspace",
    "test": "npm test -- --grep @claim:license-entitlement",
    "sandbox": "fresh browser context at /lens with only an invalid token; assert verification, locked controls, and token removal"
  },
  {
    "id": "lens-plus-price",
    "claim": "Lens Plus costs $12 as a one-time purchase.",
    "where": "landing facts, Lens Plus section, terms",
    "test": "npm test -- --grep @claim:lens-plus-price",
    "sandbox": "fresh browser context at /; assert the displayed price and one-time terms"
  },
  {
    "id": "installer-checksums",
    "claim": "The Linux install script verifies SHA-256 and leaves an executable Color Signal Lens AppImage on PATH.",
    "where": "README install section",
    "test": "npm run test:unit -- --test-name-pattern=@claim:installer-checksums",
    "sandbox": "shell fixture rejects a missing checksum, verifies a fixture AppImage, and asserts the executable survives temporary cleanup"
  },
  {
    "id": "desktop-release",
    "claim": "Each public desktop release includes macOS, Windows, Linux, SHA256SUMS, and latest.json assets.",
    "where": "README install section",
    "test": "npm run test:unit -- --test-name-pattern=@claim:desktop-release",
    "sandbox": "release-workflow regression test checks the required platform asset gate and draft-to-public ordering"
  },
  {
    "id": "macos-installer-architecture",
    "claim": "The macOS download offers the x64 installer for Intel Macs and the aarch64 installer for Apple-Silicon Macs.",
    "where": "landing install section",
    "test": "npm test -- --grep @claim:macos-installer-architecture",
    "sandbox": "fresh browser contexts with Intel and Apple-Silicon Mac user agents; mocked release response with both DMGs"
  },
  {
    "id": "macos-shell-installer-architecture",
    "claim": "The macOS install script selects the x64 DMG for Intel Macs and the aarch64 DMG for Apple-Silicon Macs.",
    "where": "README install section, install.sh",
    "test": "npm run test:unit -- --test-name-pattern=@claim:macos-shell-installer-architecture",
    "sandbox": "shell fixture lists the Apple-Silicon DMG first, mocks Darwin uname -m for x86_64, i386, arm64, and aarch64, and observes the verified filename"
  },
  {
    "id": "clear-lens",
    "claim": "Clear lens removes the overlay and restores the original screenshot.",
    "where": "workspace Clear lens action, README",
    "test": "npm test -- --grep @claim:clear-lens",
    "sandbox": "fresh demo; remap the known green pixel, clear the lens, and compare its original RGB value"
  },
  {
    "id": "license-daily-cache",
    "claim": "A current verified license is checked no more than once per day.",
    "where": "README Lens Plus section",
    "test": "npm test -- --grep @claim:license-daily-cache",
    "sandbox": "fresh context with a current token-bound valid verdict; assert paid controls load with no verification request"
  },
  {
    "id": "license-restore",
    "claim": "A purchase can be restored by pasting its license on the landing page.",
    "where": "landing Lens Plus section, README",
    "test": "npm test -- --grep @claim:license-restore",
    "sandbox": "fresh context; paste a fixture license, mock the Sociobot valid response, and assert local storage"
  },
  {
    "id": "release-fallback",
    "claim": "The landing page keeps a direct release-page fallback when GitHub metadata is unavailable.",
    "where": "README install section, landing install section",
    "test": "npm test -- --grep @claim:release-fallback",
    "sandbox": "fresh context with GitHub API blocked; assert calm status and usable direct release link"
  },
  {
    "id": "sociobot-checkout-path",
    "claim": "Lens Plus purchases use the registered Sociobot checkout path.",
    "where": "landing Lens Plus purchase action",
    "test": "npm test -- --grep @claim:sociobot-checkout-path",
    "sandbox": "fresh landing page; assert the purchase link targets the exact controller registration path"
  }
]
